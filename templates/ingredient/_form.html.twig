        
        <form action='{{(action)}}' novalidate method="POST" enctype='multipart/form-data'>
        
                <div class="form-row">  
                
                        <div class="form-group col-md-12"> 
                                <label> {{form_label(form.nom)}} <label style="color:red"> * </label> </label>
                                {{form_widget(form.nom)}}
                        </div>
                
                </div>

                <div class="d-flex justify-content-center">
                        <button class="btn btn-primary ">{{button |default('Créer')}} </button>
                </div>

        </form>
        <br><br><br><br><br>